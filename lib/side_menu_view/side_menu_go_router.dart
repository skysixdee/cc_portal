//import 'package:flutter/material.dart';


/*import 'package:flutter/material.dart';
import 'package:go_router/go_router.dart';

// Import your other necessary files
import 'side_menu_list_view.dart'; // Assuming your SideMenuListView file is in the same directory

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  final GoRouter _router = GoRouter(
    navigatorKey: GlobalKey<NavigatorState>(),
    initialLocation: '/home', // replace with your initial route
    navigatorBuilder: (context, state, stateFunc) {
      return Navigator(
        key: state.navigatorKey,
        pages: state.pages,
        onPopPage: stateFunc,
      );
    },
  );

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'SM_ADMIN_PORTAL',
      navigatorKey: _router.navigatorKey,
      onGenerateRoute: _router.navigator.onGenerateRoute,
      home: SideMenuListView(),
    );
  }
}*/