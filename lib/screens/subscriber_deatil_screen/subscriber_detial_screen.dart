import 'package:flutter/material.dart';

class SubscriberDetailScreen extends StatelessWidget {
  const SubscriberDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Subscriber detail screen"),
    );
  }
}
