import 'package:flutter/material.dart';

class ToneDetailCell extends StatelessWidget {
  const ToneDetailCell({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('ToneDetailCell'),
    );
  }
}
