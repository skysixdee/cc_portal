import 'package:flutter/material.dart';

class PackDetailCell extends StatelessWidget {
  const PackDetailCell({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('pack detail cell'),
    );
  }
}
