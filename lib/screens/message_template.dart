import 'package:flutter/material.dart';

class MessageTemplateScreen extends StatelessWidget {
  const MessageTemplateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('MessageTemplate'),
      color: Colors.white,
    );
  }
}